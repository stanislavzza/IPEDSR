% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ipeds_survey_registry.R
\name{get_survey_pattern}
\alias{get_survey_pattern}
\title{Get Survey Pattern from Registry}
\usage{
get_survey_pattern(survey_name, validate = TRUE)
}
\arguments{
\item{survey_name}{Name of the survey (see \code{list_surveys()})}

\item{validate}{If TRUE, warns if survey name not found (default: TRUE)}
}
\value{
Character string with regex pattern, or NULL if not found
}
\description{
Retrieve the regex pattern for a specific IPEDS survey type.
Use this in \code{my_dbListTables()} to get all tables for a survey.
}
\examples{
\dontrun{
# Get all faculty salary tables
pattern <- get_survey_pattern("salaries")
tables <- my_dbListTables(search_string = pattern)

# Get all enrollment tables
pattern <- get_survey_pattern("enrollment_fall")
tables <- my_dbListTables(search_string = pattern)
}

}
